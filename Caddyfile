# Agent Nexus - VPS Configuration
# Access via: http://107.174.231.22:8888

{
    admin localhost:2018
}

:8888 {
    # Enable logging
    log {
        output file /home/pets/temp/sessions_landing/caddy.log
    }

    # API routes go to backend
    handle /api/* {
        reverse_proxy localhost:18890
    }

    # WebSocket for real-time updates
    handle /ws {
        reverse_proxy localhost:18890
    }

    # Health check
    handle /health {
        reverse_proxy localhost:18890
    }

    # Everything else goes to frontend dev server
    handle {
        reverse_proxy localhost:3000
    }
}
